<header class="mb-6 px-2 py-3">
  <h1 class="text-3xl font-bold text-[#1f1b15] sm:text-4xl">
    <%= t("appointments.edit.title") %>
  </h1>
</header>

<section
  class="medisu-card p-7 sm:p-9"
  data-controller="confirmation-dialog"
>
  <%= render "delete_confirmation" %>
  <%= render "form" %>
</section>
